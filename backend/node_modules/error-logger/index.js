'use strict';

var error = {
  code,
  error,
  details,
  schema,
  table,
  column,
  constraint
};

function ErrorLogger() {
};

ErrorLogger.prototype.logDBConnectError = function logDBConnectError(error) {
  // body...
};

ErrorLogger.prototype.logDBQueryError = function logDBQueryError(error) {
  // body...
};

module.exports = ErrorLogger;
