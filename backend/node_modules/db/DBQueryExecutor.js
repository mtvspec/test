'use strict';

var config = {
  type: 'SELECT_RECORDS',
  query: {
    text: DBQueries.selectPersonsQuery.text,
    values: DBQueries.selectPersonsQuery.values
  },
  log: {
    text: DBQueries.
    values:
  }
};

function QueryExecutor(config) {
  // body...
};