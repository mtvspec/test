'use strict';

function Controller() {
};

Controller.prototype.validateID = function validateID(id) {
  var res;
  id = Number(id);
  if (id) {
    (typeof id === 'number' && id > 0) ? res = true : res = false
    console.log(res);
    return res;
  }
  return false;
};

module.exports = Controller;